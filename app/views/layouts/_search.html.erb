<div class="d-flex align-center input-group rounded" id="search" style="width: 40%">
  <input type="search" class="form-control rounded" id="search_input" placeholder="Buscar" aria-label="Buscar" aria-describedby="search-addon" style="display: none"/>
  <span class="input-group-text border-0" id="search-addon">
    <i class="fas fa-search"></i>
  </span>
</div>
<script>
  const searchInput = document.getElementById('search_input');

  searchInput.addEventListener('keypress', function(event) {
      const searchTerm = searchInput.value;
      console.log('Búsqueda realizada:', searchTerm);
      // sendSearch(searchTerm);
      function sendSearch(searchTerm) {
    const url = '/search'; // URL del controlador de búsqueda
    const data = { term: searchTerm }; // Objeto con el término de búsqueda

    fetch(url, {
      method: 'GET',
    })
    .then(response => response.json())
    .then(responseData => {
      // Hacer algo con la respuesta del controlador
      console.log('Respuesta del controlador:', responseData);
    })
    .catch(error => {
      console.error('Error en la solicitud:', error);
    });
  }
  });
</script>
